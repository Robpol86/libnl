language: python

install:
  - pip install codecov tox

script:
  - tox
  - (cd ./tests && ls .coverage.py34 .coverage.py33 .coverage.py27)

after_success:
  - mv tests/.coverage* .
  - coverage combine
  - codecov
